<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="live.combatemic.app">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="com.samsung.android.providers.context.permission.WRITE_USE_APP_FEATURE_SURVEY"/>
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <permission
        android:name="android.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/ic_launcher_foreground_final"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="HardcodedDebugMode"
        tools:targetApi="m">
        <activity
            android:name="live.combatemic.app.TermsScrollingActivity"
            android:label="@string/title_activity_terms_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="live.combatemic.app.PrivacyScrollingActivity"
            android:label="@string/title_activity_privacy_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="live.combatemic.app.AboutusActivity"
            android:label="About us" />
        <activity
            android:name="live.combatemic.app.YouTubeActivity"
            android:label="@string/title_activity_you_tube"
            android:theme="@style/AppTheme.NoActionBar" />

        <service
            android:name="live.combatemic.app.Common.MyFirebaseMessagingService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name="live.combatemic.app.CityScrollingActivity"
            android:label="@string/title_activity_city_scrolling" />
        <activity
            android:name="live.combatemic.app.QuestionAnswerActivity"
            android:label="@string/menu_faq" />
        <activity
            android:name="live.combatemic.app.ContactActivity"
            android:theme="@style/AppTheme" />
        <activity android:name="live.combatemic.app.ProfileActivity" />
        <activity
            android:name="live.combatemic.app.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:theme="@style/SwitchTheme"/>
        <activity android:name="live.combatemic.app.RegistrationActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="live.combatemic.app.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name="live.combatemic.app.SideBarActivity"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="true"
            android:theme="@style/AppTheme.NoActionBar"
            tools:targetApi="n">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name="live.combatemic.app.ProfileFragment" />
        <activity
            android:name="live.combatemic.app.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name="live.combatemic.app.AlarmReceiver" />

        <service android:name="live.combatemic.app.LocationTrack" />
    </application>

</manifest>