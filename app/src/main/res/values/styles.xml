<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorBackground</item>
        <item name="colorPrimaryDark">@color/colorBackground</item>
        <item name="colorAccent">@android:color/white</item>
        <!--        <item name="android:textColorPrimary">@android:color/white</item>-->
        <item name="android:textColorSecondary">@android:color/black</item>
        <item name="android:fontFamily">@font/custom_font</item>
        <!--        <item name="android:windowBackground">@color/colorBackground</item>-->
    </style>

    <style name="ToolbarColoredBackArrow" parent="AppTheme.PopupOverlay">
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppCompatAlert" parent="ThemeOverlay.AppCompat.Dialog.Alert">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@color/colorBackground</item>
        <!--        <item name="colorAccent">@color/colorBackground</item>-->
    </style>

    <!-- ToolBar -->
    <style name="ToolBarStyle" parent="Widget.AppCompat.Toolbar">
        <!--        <item name="android:fontFamily">@font/pacifico</item>-->
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="actionMenuTextColor">@android:color/white</item>
    </style>

    <style name="SCBSwitch" parent="AppTheme">
        <!-- active thumb & track color (30% transparency) -->
        <item name="android:colorControlActivated">#063435</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">#D50C0C</item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#42ED0808</item>
    </style>

    <style name="SwitchTheme" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@android:color/darker_gray</item>
    </style>

    <style name="PreferencesTheme">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:background">#FFEAEAEA</item>
    </style>

</resources>
